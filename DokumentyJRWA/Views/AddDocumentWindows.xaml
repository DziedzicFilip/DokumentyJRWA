<Window x:Class="DokumentyJRWA.Views.AddDocumentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dodaj dokument" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Margin="0,5" 
         Text="{Binding Tytul, UpdateSourceTrigger=PropertyChanged}" />
<DatePicker Grid.Row="1" Margin="0,5" 
            SelectedDate="{Binding DataWplywu, Mode=TwoWay}"/>
<TextBox Grid.Row="2" Margin="0,5" 
         Text="{Binding JrwaCode, UpdateSourceTrigger=PropertyChanged}" />
<TextBox Grid.Row="3" Margin="0,5" 
         Text="{Binding Podmioty, UpdateSourceTrigger=PropertyChanged}" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,5">
            <TextBox Width="200" 
                     Text="{Binding FilePath, Mode=TwoWay}" 
                     IsReadOnly="True"/>
            <Button Content="Wybierz plik" 
                    Command="{Binding ChooseFileCommand}" 
                    Margin="5,0"/>
        </StackPanel>
        <Button Grid.Row="5" Content="Dodaj" 
                IsDefault="True"
                Click="Add_Click" 
                Margin="0,10,0,0" 
                HorizontalAlignment="Right" Width="100"/>
    </Grid>
</Window>